# Folder Structure

Your bundle includes one or more of these folders:

- `web/` — Web icons
- `desktop/` — Desktop PNGs (+ `.ico` and `.icns` when available)
- `mobile/`
  - `ios-icons/`
  - `android-icons/`
- `svg/` — Original SVG
